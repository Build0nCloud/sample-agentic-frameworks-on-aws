[project]
name = "dynamodb-mcp"
version = "0.1.0"
description = "DynamoDB MCP server for AgentCore Gateway - medical records demo"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.0.0",
    "uvicorn>=0.30.0",
    "boto3>=1.35.0",
]

[tool.uv]
dev-dependencies = []
