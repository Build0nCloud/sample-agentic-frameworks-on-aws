[project]
name = "jira-mock-mcp"
version = "0.1.0"
description = "Mock Jira MCP server for AgentCore Gateway demos"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.0.0",
    "uvicorn>=0.30.0",
]

[tool.uv]
dev-dependencies = []
